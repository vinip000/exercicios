#include <iostream>
#include <locale.h>
#include "carros.hpp"
using namespace std;

void cadastrar_carros(Carro* carro, int tamananho);
int ano;
string nome;
string marca;
float preco;
for (int i = 0; i < tamanho; i++) {
  cout << "Digite o nome do carro: ";
  cin >> nome;
  cout << "Digite a marca do carro: ";
  cin >> marca;
  cout << "Digite o ano do carro: ";
  cin >> ano;
  cout << "Digite o preço do carro: ";
  cin >> preco;
  Carro x;
  carros[i] = x
    x.nome = nome; x.marca = marca; x.ano = ano; x.preco = preco; carros[i] = x
}

void contar_ano(Carro * carro, int tamananho) {
  for (int i = 0; i < tamanho; i++) {
    if ()
  }
}